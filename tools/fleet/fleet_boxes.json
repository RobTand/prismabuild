{
  "schema": "prismaquant.prismabuild.fleet_boxes.v1",
  "_why": "One versioned statement of what each box offers the pool, so a supervisor on any box can be started by an identical command line and the shape of the fleet is a file somebody can read rather than three hand-typed invocations nobody wrote down. Values here are the ones the loops were observed running on 2026-09-03; changing the fleet means changing this file.",
  "boxes": {
    "sparky": {
      "loops": 5,
      "args": ["--class", "gb10", "--gpu-slots", "2", "--mem-gb", "48",
               "--timeout-s", "7200", "--max-idle", "500", "--poll-s", "15"]
    },
    "gx10-6b77": {
      "_alias": "sparklina",
      "loops": 3,
      "args": ["--class", "gb10", "--tag", "sparklina", "--gpu-slots", "1",
               "--mem-gb", "40", "--timeout-s", "7200", "--max-idle", "500",
               "--poll-s", "20", "--python",
               "/home/rob/dq-runs/venvs/prismaquant-cu130/bin/python"]
    },
    "dl380g10": {
      "loops": 16,
      "args": ["--class", "x86", "--gpu-slots", "0", "--mem-gb", "60",
               "--all-cores", "--timeout-s", "3600", "--max-idle", "2000",
               "--poll-s", "10", "--python", "/home/rob/venvs/pb-cpu/bin/python"]
    }
  }
}
