#!/bin/sh
# A local guard, not a replacement for GitHub branch protection.
while read -r local_ref local_oid remote_ref remote_oid; do
    if [ "$remote_ref" = refs/heads/main ]; then
        echo 'Direct pushes to main are blocked. Use a branch, linked issue and pull request.' >&2
        exit 1
    fi
done
